<UserControl x:Class="LogoUI.Samples.Client.Gui.Shell.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModels1="clr-namespace:LogoUI.Samples.Client.Gui.Shell.ViewModels"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance viewModels1:MainViewModel}"
             d:DesignHeight="300"
             d:DesignWidth="300">

    <UserControl.InputBindings>
        <KeyBinding Key="BrowserBack" Command="NavigationCommands.BrowseBack" />
        <KeyBinding Key="BrowserForward" Command="NavigationCommands.BrowseForward" />
    </UserControl.InputBindings>           
    <Grid>
        <Grid.RowDefinitions>            
            <RowDefinition Height="Auto" />            
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" Text="Hello Login"/>
    </Grid>
</UserControl>
